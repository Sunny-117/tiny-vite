export type Student = {
  id: number;
  name: string;
  birthday: string;
  grade: string;
  score: number;
}
export type User = {
  id: number;
  name: string;
  role: string;
  level: string;
  tel: string;
}
export type Column = {
  prop: string;
  label: string;
}